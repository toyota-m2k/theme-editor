<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ThemeEditor.ColorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ThemeEditor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Title="Stocked Colors">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左ペイン: 種別選択・操作・履歴リスト -->
        <Grid Grid.Column="0" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <controls:Segmented
                x:Name="TypeSelector"
                Grid.Row="0"
                SelectedIndex="0"
                SelectionMode="Single"
                HorizontalAlignment="Left">
                <controls:SegmentedItem Content="Primary"/>
                <controls:SegmentedItem Content="Secondary"/>
                <controls:SegmentedItem Content="Tertiary"/>
            </controls:Segmented>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,10">
                <Button Content="Stock" Click="OnStockClick"/>
                <Button x:Name="ApplyButton" Content="Apply" Click="OnApplyClick" IsEnabled="False"/>
                <Button x:Name="RemoveButton" Content="Remove" Click="OnRemoveClick" IsEnabled="False"/>
            </StackPanel>

            <ListView
                x:Name="StockList"
                Grid.Row="2"
                ItemsSource="{x:Bind ViewModel.Stocker.StockedThemes, Mode=OneWay}"
                SelectionChanged="OnSelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:StockItem">
                        <StackPanel Orientation="Vertical" Padding="2,6">
                            <TextBlock Text="{x:Bind Title}" FontSize="14"/>
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="{x:Bind Type}" FontSize="11" Opacity="0.7"/>
                                <TextBlock Text="{x:Bind CreatedAt}" FontSize="11" Opacity="0.7"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- 右ペイン: 選択項目のプレビュー -->
        <Grid Grid.Column="1" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="PreviewTitle" Grid.Row="0" FontSize="16" Margin="0,0,0,10"/>
            <ScrollViewer
                Grid.Row="1"
                HorizontalScrollMode="Auto"
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto">
                <Grid x:Name="PreviewGrid"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
